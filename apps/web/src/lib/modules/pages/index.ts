// Universal barrel: types and slug constants only — services live in ./server.
export {
	COOKIE_PAGE_SLUG,
	LEGAL_PAGE_SLUGS,
	PRIVACY_PAGE_SLUG,
	TERMS_PAGE_SLUG
} from './seed-pages.ts';
export type { PageRow } from './schema.ts';
